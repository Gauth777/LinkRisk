# Data

Do not commit the IEEE-CIS competition files. Download them from Kaggle after accepting the competition rules.

Place:
- `data/raw/train_transaction.csv`
- `data/raw/train_identity.csv`

The join key is `TransactionID`.

Important: many identity fields are masked. Do not rename a masked field to a precise concept unless the provider explicitly documents that mapping.
