# v0.9 result note

Validation result: FAIL as a fixed-priority capacity router.

The failure is informative. The v0.9 ordering placed trusted-fraud VERIFY overrides ahead of Mentalist and score-band VERIFY. On validation, routing-source fraud rates were approximately 2.99% for trusted overrides, 10.43% for Mentalist proactive cases, and 24.50% for remaining v0.5 score-band VERIFY cases. The router therefore displaced higher-yield current-risk cases with lower-yield historical-prior cases.

This does not overturn v0.8: Mentalist still contributed 50 frauds among 519 transactions that the full v0.5 policy would otherwise ALLOW. The next experiment tests label-free one-for-one capacity reallocation rather than fixed evidence priority.

This note is development interpretation only; the held-out test remains sealed.
